# Audit Report - 2026-04-08

## Summary
- 🔴 Critical Issues: 2
- 🟡 Warnings: 4
- 🟢 Suggestions: 3

## 🔴 Critical Issues (Phải sửa ngay)
1. **Lỗ hổng bảo mật XSS (Hacker có thể chiếm quyền điều khiển)**
   - File: `file:///d:/git/trading-portfolio-pro/ui.js`
   - Nguy hiểm: Ứng dụng đang hiển thị Tên mã (Symbol) và Ghi chú (Note) bằng cách dán trực tiếp vào HTML (`innerHTML`). Nếu anh vô tình nhập hoặc Import một file JSON độc hại, kẻ tấn công có thể chạy mã script để lấy API Key hoặc dữ liệu của anh.
   - Cách sửa: Sử dụng `.textContent` hoặc thực hiện "lọc" (Sanitize) các ký tự đặc biệt trước khi hiển thị.

2. **Sai tên Model AI mặc định (Dẫn đến lỗi không gọi được AI)**
   - File: `file:///d:/git/trading-portfolio-pro/ai.js` (Dòng 21)
   - Nguy hiểm: Tên model đang để là `gemini-2.5-flash` (không tồn tại trong danh sách model công khai hiện tại), dẫn đến việc trợ lý AI sẽ báo lỗi "không tìm thấy" nếu anh không tự chọn model khác trong Settings.
   - Cách sửa: Sửa giá trị mặc định thành `gemini-1.5-flash`.

## 🟡 Warnings (Nên sửa)
1. **Trùng lặp Logic tính toán (Dễ gây sai sót khi cập nhật)**
   - Vấn đề: Logic chia tỷ giá 1000 cho chứng khoán Việt Nam đang được viết lặp lại ở 3-4 chỗ khác nhau (`popup.js`, `ui.js`).
   - Hậu quả: Nếu sau này anh muốn thêm sàn mới hoặc đổi cách tính, anh sẽ phải đi sửa từng file, rất dễ bỏ sót.

2. **Thiếu kiểm tra dữ liệu khi Import**
   - Vấn đề: Khi anh bấm Import JSON, hệ thống chỉ kiểm tra nó có phải là "danh sách" không mà không kiểm tra nội dung bên trong có đúng cấu trúc Trade không.
   - Hậu quả: Nếu file JSON bị lỗi, app có thể bị "treo" hoặc không hiển thị được danh mục.

3. **Fetch giá quá thường xuyên**
   - Vấn đề: App tự động load lại giá toàn bộ danh mục mỗi 5 giây.
   - Hậu quả: Nếu danh mục quá dài, TradingView có thể tạm thời chặn (Rate limit) yêu cầu từ máy anh.

## 🟢 Suggestions (Tùy chọn)
1. **Chia nhỏ file code**: File `ui.js` đang quá dài (>800 dòng), nên chia nhỏ ra để dễ quản lý và sửa lỗi sau này.
2. **Đồng bộ đặt tên**: Một số biến dùng `camelCase`, một số dùng `snake_case`, nên thống nhất một kiểu (khuyên dùng camelCase cho Javascript).
3. **Thêm thông báo (Toast)**: Khi lưu, xóa hoặc import thành công, nên có một thông báo nhỏ ở góc màn hình thay vì dùng `alert()` kiểu cũ.

## Next Steps
1. Sửa lỗi XSS để đảm bảo an toàn API Key.
2. Cập nhật model AI mặc định.
3. Refactor logic chia tỷ giá vào một module chung.
